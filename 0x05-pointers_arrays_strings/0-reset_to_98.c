#include "main.h"
#include <stdio.h>

/**
 * main - a function that takes a pointer to an integer
 * as parameter and updates the value it points
 * to 98.
 *
 * Return: void.
 */

int main(void)
{
	int n;

	n = 98;
	modif_my_param(n);

       return (void);
}
