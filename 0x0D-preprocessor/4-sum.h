#ifndef _SUM_LIKE_MACRO_
#define _SUM_LIKE_MACRO_

/**
 * main - Write a macro like function that computes
 * the sum of the numbers x and y.
 * 
 * Return: Always 0.
 */

#define SUM(x, y) ((x) + (y))

#endif
